import CompanionChat from '@/components/CompanionChat';

// /companion — new chat (no conversationId) renders the welcome screen
export default function CompanionPage() {
  return <CompanionChat />;
}
